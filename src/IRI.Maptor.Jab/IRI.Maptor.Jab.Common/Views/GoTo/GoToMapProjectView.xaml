<UserControl x:Class="IRI.Maptor.Jab.Controls.GoToMapProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:mahApp="http://metro.mahapps.com/winfx/xaml/controls"
            xmlns:localization="clr-namespace:IRI.Maptor.Jab.Core.Localization;assembly=IRI.Maptor.Jab.Core" 
             x:Name="root">
    <UserControl.Resources>
        <ObjectDataProvider x:Key="Localization" ObjectInstance="{x:Static localization:LocalizationManager.Instance}"/>
    </UserControl.Resources>
    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding [srs_utmZone], Source={StaticResource Localization}}" Margin="4" VerticalAlignment="Center"/>
        <!--<TextBlock Text="Zone" Margin="4" VerticalAlignment="Center"/>-->
        <mahApp:NumericUpDown Grid.Column="1" Minimum="0" Maximum="60"
                              
                              Style="{StaticResource IRI.Maptor.Styles.NumericUpDown.Small}" 
                              Value="{Binding UtmZone, Mode=TwoWay, StringFormat={}{0:00}}" MinWidth="75"/>
        
        <TextBlock Grid.Row="1" Text="X" Margin="4" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding X, Mode=TwoWay}" 
                 Style="{StaticResource IRI.Maptor.Styles.TextBox.Small.En}" BorderThickness="1"/>
        
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Y" Margin="4" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Y, Mode=TwoWay}"
                 Style="{StaticResource IRI.Maptor.Styles.TextBox.Small.En}" BorderThickness="1"/>
    </Grid>
</UserControl>
