<UserControl x:Class="IRI.Maptor.Jab.Controls.Symbology.Sld.SimpleFilterEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mahApp="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <CheckBox Grid.Row="0" Margin="4" 
                  Content="{Binding [sldEditor_filter_enableFilter], Source={StaticResource Localization}}" 
                  IsChecked="{Binding HasFilter, Mode=TwoWay}"/>

        <GroupBox Grid.Row="1" Header="{Binding [sldEditor_filter_filterSettings], Source={StaticResource Localization}}" Margin="4" 
                  IsEnabled="{Binding HasFilter}">
            <StackPanel>
                <TextBlock Text="{Binding [sldEditor_filter_filterExpression], Source={StaticResource Localization}}" Margin="4"/>
                <TextBox Margin="4" Height="60" TextWrapping="Wrap" 
                         AcceptsReturn="True" 
                         VerticalScrollBarVisibility="Auto"
                         Text="{Binding FilterDescription, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         IsReadOnly="True"/>
                <TextBlock Margin="4" FontStyle="Italic" FontSize="10" Foreground="Gray"
                           Text="{Binding [sldEditor_filter_simplifiedViewNote], Source={StaticResource Localization}}"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>

