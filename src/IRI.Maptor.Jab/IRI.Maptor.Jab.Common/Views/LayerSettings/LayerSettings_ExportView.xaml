<UserControl x:Class="IRI.Maptor.Jab.Controls.LayerSettings.LayerSettings_ExportView"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
                xmlns:mahApp="http://metro.mahapps.com/winfx/xaml/controls"
                xmlns:maptorDsKind="clr-namespace:IRI.Maptor.Sta.Common.Enums;assembly=IRI.Maptor.Sta.Common"
                xmlns:maptorCommon="clr-namespace:IRI.Maptor.Jab.Common" 
                xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                xmlns:localization="clr-namespace:IRI.Maptor.Jab.Common.Localization" >
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/IRI.Maptor.Jab.Common;component/Assets/Styles/SecurityInputStyles.xaml"/>
                <ResourceDictionary Source="/IRI.Maptor.Jab.Common;component/Assets/IRI.Maptor.Converters.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/IRI.Maptor.Jab.Common;component/Assets/Styles/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <ObjectDataProvider x:Key="Localization" ObjectInstance="{x:Static localization:LocalizationManager.Instance}"/>
            <Style x:Key="cardBorder" TargetType="Border">
                <Setter Property="BorderBrush" Value="Gray"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Background" Value="White"/>
                <Setter Property="Padding" Value="4,0"/>
                <Setter Property="Margin" Value="4"/>
                <Setter Property="CornerRadius" Value="5"/>
                <Setter Property="Height" Value="39"/>
            </Style>
            <Style TargetType="RadioButton" BasedOn="{StaticResource MahApps.Styles.RadioButton}">
                <Setter Property="Margin" Value="4,0"/>
                <Setter Property="FontFamily" Value="{StaticResource iranSans}"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>
            <Style TargetType="mahApp:ToggleSwitch" BasedOn="{StaticResource MahApps.Styles.ToggleSwitch}">
                <Setter Property="FontFamily" Value="{StaticResource iranSans}"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!--export format type-->
        <Border Grid.Row="0" Style="{StaticResource cardBorder}">
            <StackPanel Orientation="Horizontal">
                <iconPacks:PackIconMaterial Kind="File" Style="{StaticResource securityIconMaterial}"/>
                <TextBlock Text="{Binding [dialog_export_outputFileFormat], Source={StaticResource Localization}}" FontWeight="Bold" VerticalAlignment="Center" Margin="4"/>
                <RadioButton GroupName="A" IsChecked="{Binding SelectedDataSourceKind, Converter={StaticResource enumToBoolConverter}, ConverterParameter={x:Static maptorDsKind:DataSourceKind.Shapefile}}" Content="SHP" />
                <RadioButton GroupName="A" IsChecked="{Binding SelectedDataSourceKind, Converter={StaticResource enumToBoolConverter}, ConverterParameter={x:Static maptorDsKind:DataSourceKind.Dxf}}" Content="DXF"/>
                <RadioButton GroupName="A" IsChecked="{Binding SelectedDataSourceKind, Converter={StaticResource enumToBoolConverter}, ConverterParameter={x:Static maptorDsKind:DataSourceKind.Kml}}" Content="KML"/>
                <RadioButton GroupName="A" IsChecked="{Binding SelectedDataSourceKind, Converter={StaticResource enumToBoolConverter}, ConverterParameter={x:Static maptorDsKind:DataSourceKind.Kmz}}" Content="KMZ"/>
                <RadioButton GroupName="A" IsChecked="{Binding SelectedDataSourceKind, Converter={StaticResource enumToBoolConverter}, ConverterParameter={x:Static maptorDsKind:DataSourceKind.GeoJson}}" Content="GeoJson"/>
                <RadioButton GroupName="A" IsChecked="{Binding SelectedDataSourceKind, Converter={StaticResource enumToBoolConverter}, ConverterParameter={x:Static maptorDsKind:DataSourceKind.EsriJson}}" Content="EsriJson"/>
                <!--<RadioButton GroupName="A" IsChecked="{Binding SelectedDataSourceKind, Converter={StaticResource enumToBoolConverter}, ConverterParameter={x:Static maptorDsKind:DataSourceKind.TopoJson}}" Content="TopoJson"/>-->
            </StackPanel>
        </Border>

        <!--what to export-->
        <Border Grid.Row="1" Style="{StaticResource cardBorder}">
            <StackPanel Orientation="Horizontal">
                <iconPacks:PackIconMaterial Kind="SelectMarker" Style="{StaticResource securityIconMaterial}"/>
                <TextBlock Text="{Binding [dialog_export_exportModeTItle], Source={StaticResource Localization}}" FontWeight="Bold" VerticalAlignment="Center" Margin="4"/>
                <RadioButton GroupName="B"
                             IsChecked="{Binding SelectedExportMode, Converter={StaticResource enumToBoolConverter}, ConverterParameter={x:Static maptorCommon:ExportFeatureMode.All}}"
                             Content="{Binding [dialog_export_exportModeAll], Source={StaticResource Localization}}"/>
                <RadioButton GroupName="B" IsEnabled="False"
                             IsChecked="{Binding SelectedExportMode, Converter={StaticResource enumToBoolConverter}, ConverterParameter={x:Static maptorCommon:ExportFeatureMode.Selected}}"
                             Content="{Binding [dialog_export_exportModeSelected], Source={StaticResource Localization}}"/>
                <RadioButton GroupName="B" IsEnabled="False"
                             IsChecked="{Binding SelectedExportMode, Converter={StaticResource enumToBoolConverter}, ConverterParameter={x:Static maptorCommon:ExportFeatureMode.Highlighted}}"
                             Content="{Binding [dialog_export_exportModeHighlighted], Source={StaticResource Localization}}"/>
            </StackPanel>
        </Border>


        <!-- Spatial Reference -->
        <Border Grid.Row="2" Style="{StaticResource cardBorder}"
                Visibility="{Binding ShowSrsOptions, Converter={StaticResource boolToVisibilityConverter}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="32"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <iconPacks:PackIconPhosphorIcons Kind="VectorTwo" Style="{StaticResource securityIconPhosphor}"/>
                <mahApp:ToggleSwitch Margin="4" Grid.Column="1"
                                     OnContent="{Binding [dialog_export_enabledProjection], Source={StaticResource Localization}}"
                                     OffContent="{Binding [dialog_export_disabledProjection], Source={StaticResource Localization}}"
                                     IsOn="{Binding IsProjectionEnabled}"/>
              
            </Grid>
        </Border>

        <Border Grid.Row="3" Style="{StaticResource cardBorder}"
                Visibility="{Binding IsProjectionEnabled, Converter={StaticResource boolToVisibilityConverter}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="32"/>
                    <ColumnDefinition Width="*"/> 
                </Grid.ColumnDefinitions>
                <iconPacks:PackIconPhosphorIcons Grid.Column="0" Kind="Globe" Style="{StaticResource securityIconPhosphor}"/>
                <ComboBox Grid.Column="1" Margin="4" BorderThickness="0" FontSize="15" MinHeight="35"
                            VerticalAlignment="Center" VerticalContentAlignment="Center" HorizontalAlignment="Stretch"
                            FlowDirection="LeftToRight"
                            IsEnabled="{Binding IsProjectionEnabled}"
                            ItemsSource="{Binding AvailableSrsOptions}"
                            DisplayMemberPath="DisplayName"
                            SelectedItem="{Binding SelectedSrsOption, Mode=TwoWay}"/>
            </Grid>
        </Border>
            <!-- UTM Zone (visible when UTM selected) -->
        <Border Grid.Row="4" Style="{StaticResource cardBorder}">
            <!--Visibility="{Binding IsUtmSelected, Converter={StaticResource boolToVisibilityConverter}}">-->
            <Border.Visibility>
                <MultiBinding Converter="{StaticResource multiBooleanToVisibilityConverter}">
                    <Binding Path="IsProjectionEnabled"/>
                    <Binding Path="IsUtmSelected"/>
                </MultiBinding>
            </Border.Visibility>
            <Grid Margin="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="32"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <iconPacks:PackIconPhosphorIcons Kind="MapTrifoldFill" Style="{StaticResource securityIconPhosphor}"/>
                <mahApp:NumericUpDown Grid.Column="1" Minimum="1" Maximum="60" BorderThickness="0"
                                   Value="{Binding UtmZone, Mode=TwoWay}" Margin="4" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="2" VerticalAlignment="Center" Margin="8,2,4,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontFamily" Value="{StaticResource iranSans}"/>
                            <!-- Default (northern hemisphere) -->
                            <Setter Property="Text" 
                                    Value="{Binding [srs_northernHemisphere], Source={StaticResource Localization}}"/>
                            <Style.Triggers>
                                <!-- Southern hemisphere (IsChecked = False) -->
                                <DataTrigger Binding="{Binding IsNorthHemisphere}" Value="False">
                                    <Setter Property="Text" 
                                            Value="{Binding [srs_southernHemisphere], Source={StaticResource Localization}}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <ToggleButton Grid.Column="3" IsChecked="{Binding IsNorthHemisphere, Mode=TwoWay}"
                            Style="{StaticResource IRI.Maptor.Styles.ToggleButton.CircleLight}" FlowDirection="LeftToRight"
                            ToolTip="{Binding [dialog_dxfOpen_hemisphereTooltip], Source={StaticResource Localization}}">
                    <iconPacks:PackIconPhosphorIcons Kind="CircleHalfFill" RenderTransformOrigin="0.5,0.5"
                                                  Margin="0" Width="24" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <iconPacks:PackIconPhosphorIcons.Style>
                            <Style TargetType="iconPacks:PackIconPhosphorIcons" BasedOn="{StaticResource securityIconPhosphor}">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="-90"/>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsNorthHemisphere}" Value="False">
                                        <Setter Property="RenderTransform">
                                            <Setter.Value>
                                                <RotateTransform Angle="90"/>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </iconPacks:PackIconPhosphorIcons.Style>
                    </iconPacks:PackIconPhosphorIcons>
                </ToggleButton>

            </Grid>
        </Border>

        <!-- Options: X,Y vs Y,X -->
        <Border Grid.Row="5" Style="{StaticResource cardBorder}"
                Visibility="{Binding ShowXYOrderOptions, Converter={StaticResource boolToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal">
                <iconPacks:PackIconPhosphorIcons Kind="ListNumbers" Style="{StaticResource securityIconPhosphor}"/>
                <TextBlock Text="{Binding [dialog_common_xyOrder], Source={StaticResource Localization}}"
                         VerticalAlignment="Center" Margin="8,0,0,0" FontWeight="Bold"/>
                <CheckBox Content="{Binding [dialog_common_eastingFirst], Source={StaticResource Localization}}"
                          IsChecked="{Binding IsLongitudeFirst, Mode=TwoWay}" Margin="8,0,16,0" VerticalAlignment="Center"/>
                <!--<RadioButton Content="X,Y" IsChecked="{Binding IsLongitudeFirst, Mode=TwoWay}" Margin="4,0" VerticalAlignment="Center"
                       ToolTip="{Binding [dialog_geoJsonTopoJsonOpen_orderXY], Source={StaticResource Localization}}"/>
              <RadioButton Content="Y,X" IsChecked="{Binding IsLongitudeFirst, Mode=TwoWay, Converter={StaticResource notConverter}}" Margin="4,0" VerticalAlignment="Center"
                       ToolTip="{Binding [dialog_geoJsonTopoJsonOpen_orderYX], Source={StaticResource Localization}}"/>-->
            </StackPanel>
        </Border>

        <Button Margin="4" Grid.Row="6" HorizontalAlignment="Right" 
                ToolTip="{Binding [dialog_common_export], Source={StaticResource Localization}}" 
                Style="{StaticResource IRI.Maptor.Styles.Button.Dialogs.Accent}"
                Command="{Binding ExportCommand}">
            <StackPanel Orientation="Horizontal">
                <iconPacks:PackIconMaterial Kind="FileExport" Margin="0,0,8,0" VerticalAlignment="Center" />
                <TextBlock Text="{Binding [dialog_common_export], Source={StaticResource Localization}}" VerticalAlignment="Center" Margin="2,0" />
            </StackPanel>
        </Button>

    </Grid>
</UserControl>
