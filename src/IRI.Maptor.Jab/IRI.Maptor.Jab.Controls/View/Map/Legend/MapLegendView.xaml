<userControl:NotifiableUserControl x:Class="IRI.Maptor.Jab.Controls.View.MapLegendView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"  
            xmlns:userControl="clr-namespace:IRI.Maptor.Jab.Common;assembly=IRI.Maptor.Jab.Common" 
            xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
            xmlns:localization="clr-namespace:IRI.Maptor.Jab.Common.Localization;assembly=IRI.Maptor.Jab.Common"
                                       xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
            x:Name="root"
            xmlns:maptor="clr-namespace:IRI.Maptor.Jab.Controls.View"  >
    <userControl:NotifiableUserControl.Resources>
        <ObjectDataProvider x:Key="Localization" ObjectInstance="{x:Static localization:LocalizationManager.Instance}"/>
        <Style  TargetType="{x:Type Button}" x:Key="menuCircleButton">
            <Setter Property="Margin" Value="1"/>
            <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Accent3}"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Grid>
                            <Ellipse Margin="1" Fill="{TemplateBinding Property=Background}"/>
                            <ContentPresenter/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Accent}"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Border.NonActive}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="{x:Type iconPacks:PackIconModern}">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Width" Value="16"/>
            <Setter Property="FlowDirection" Value="LeftToRight"/>
        </Style>
        <CollectionViewSource x:Key="collectionViewSource" 
                              Source="{Binding Layers, RelativeSource={RelativeSource AncestorType=userControl:NotifiableUserControl}}" 
                              Filter="CollectionViewSource_Filter">
            <CollectionViewSource.SortDescriptions>
                <scm:SortDescription PropertyName="TocOrder"/>
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>
        <HierarchicalDataTemplate x:Key="legendTemplate" ItemsSource="{Binding SubLayers}">
            <HierarchicalDataTemplate.Resources>
                <Style TargetType="ToggleButton">
                    <Setter Property="BorderThickness" Value="4,0,0,0"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ToggleButton">
                                <Border BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalAlignment}" VerticalAlignment="{TemplateBinding VerticalAlignment}"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.Highlight}"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Accent4}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.Highlight}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="LightGray"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </HierarchicalDataTemplate.Resources>

            <RadioButton Style="{StaticResource {x:Type ToggleButton}}" 
                        GroupName="{Binding ElementName=root, Path=GroupName}"
                        HorizontalAlignment="Stretch" 
                        DataContext="{Binding }" 
                        IsEnabled="{Binding IsInScaleRange}" 
                        IsChecked="{Binding IsSelectedInToc, Mode=TwoWay}">
                <RadioButton.InputBindings>
                    <MouseBinding Gesture="LeftDoubleClick" Command="{Binding ToggleExpandCommand}"/>
                </RadioButton.InputBindings>
                <maptor:MapLegendItemView Title="{Binding LayerName}" 
                                    Symbology="{Binding DefaultSymbology}"  
                                    HorizontalAlignment="Stretch"
                                    Commands="{Binding Commands}"
                                    IsChecked="{Binding IsChecked, RelativeSource={RelativeSource AncestorType=RadioButton}}"
                                    TitleFontSize="{Binding ElementName=root, Path=TitleFontSize}"/>
            </RadioButton>
        </HierarchicalDataTemplate>
    </userControl:NotifiableUserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"
                    Visibility="{Binding ElementName=root, Path=ShowTools, Converter={StaticResource boolToVisibilityConverter}}">
            <Button Style="{StaticResource menuCircleButton}"  Cursor="Hand" Command="{Binding AddGeoJsonToDrawingItemsCommand}"  
                    Focusable="False" ToolTip="{Binding [cmd_legend_addGeoJson], Source={StaticResource Localization}}" IsEnabled="False">
                <Path Data="{StaticResource json}" Stretch="Uniform" Fill="White" Width="18" Height="18" FlowDirection="LeftToRight"/>
            </Button>
            <Button Style="{StaticResource menuCircleButton}" Cursor="Hand" Command="{Binding AddCsvCommand}"
                    Focusable="False" ToolTip="{Binding [cmd_legend_addLatLongTxt], Source={StaticResource Localization}}" IsEnabled="False">
                <Path Data="{StaticResource txt}" Stretch="Uniform" Fill="White" Width="18" Height="18" FlowDirection="LeftToRight"/>
            </Button>
            <Button Style="{StaticResource menuCircleButton}" Cursor="Hand" Command="{Binding AddShapefileCommand}"
                    Focusable="False" ToolTip="{Binding [cmd_legend_addShapefile], Source={StaticResource Localization}}">
                <Path Data="{StaticResource shapefile}" Stretch="Uniform" Fill="White" Width="18" Height="18" FlowDirection="LeftToRight"/>
            </Button>
            <Button Style="{StaticResource menuCircleButton}" Cursor="Hand" Command="{Binding RemoveAllDrawingItemsCommand}" IsEnabled="False"
                    Focusable="False" ToolTip="{Binding [cmd_legend_removeAll], Source={StaticResource Localization}}">
                <iconPacks:PackIconModern Kind="Delete" />
            </Button>
            <Button Style="{StaticResource menuCircleButton}" Cursor="Hand" Command="{Binding MoveDrawingItemDownCommand}" Visibility="Collapsed"
                    Focusable="False" ToolTip="{Binding [cmd_legend_moveDown], Source={StaticResource Localization}}" IsEnabled="{Binding CanMoveDrawingItemDown}">
                <iconPacks:PackIconModern Kind="LayerDown" />
            </Button>
            <Button Style="{StaticResource menuCircleButton}" Cursor="Hand" Command="{Binding MoveDrawingItemUpCommand}" Visibility="Collapsed"
                    Focusable="False" ToolTip="{Binding [cmd_legend_moveUp], Source={StaticResource Localization}}" IsEnabled="{Binding CanMoveDrawingItemUp}">
                <iconPacks:PackIconModern Kind="LayerUp" />
            </Button>
        </StackPanel>
        <Line Grid.Row="1" X1="0" X2="100" StrokeThickness="2" Stroke="{DynamicResource MahApps.Brushes.Highlight}" 
                Visibility="{Binding ElementName=root, Path=ShowTools, Converter={StaticResource boolToVisibilityConverter}}"
                Opacity="1" Margin="0,4"
                Stretch="Fill"/>

        <TreeView ItemsSource="{Binding Source={StaticResource collectionViewSource}}" ItemTemplate="{StaticResource legendTemplate}"
                  Grid.Row="2">
            <!--<TreeView.Resources>
                <Style TargetType="TreeViewItem" BasedOn="{StaticResource {x:Type TreeViewItem}}">
                    <Setter Property="HorizontalContentAlignment" Value="Left" />
                </Style>
            </TreeView.Resources>-->
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem" BasedOn="{StaticResource {x:Type TreeViewItem}}">
                    <Setter Property="IsExpanded" Value="{Binding IsExpandedInToc, Mode=TwoWay}" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TreeViewItem">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" MinWidth="19" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition />
                                        </Grid.RowDefinitions>
                                        <ToggleButton IsChecked="{Binding Path=IsExpanded, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press" Name="Expander"
                                                      Margin="0" Padding="0">
                                            <ToggleButton.Style>
                                                <Style TargetType="ToggleButton">
                                                    <Setter Property="UIElement.Focusable" Value="false" />
                                                    <Setter Property="FrameworkElement.Width" Value="16" />
                                                    <Setter Property="FrameworkElement.Height" Value="16" />
                                                    <Setter Property="Control.Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="ToggleButton">
                                                                <Grid Width="16" Height="16" Background="Transparent">
                                                                    <Viewbox Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                                        <Path Fill="{DynamicResource MahApps.Brushes.Accent}" Name="ExpandPath" >
                                                                            <Path.Data>
                                                                                <PathGeometry Figures="M8.59,16.58L13.17,12L8.59,7.41L10,6L16,12L10,18L8.59,16.58Z" />
                                                                            </Path.Data>
                                                                            <Path.RenderTransform>
                                                                                <!--<RotateTransform Angle="135" CenterX="3" CenterY="3" />-->
                                                                                <RotateTransform Angle="0" CenterX="0" CenterY="0" />
                                                                            </Path.RenderTransform>
                                                                        </Path>
                                                                    </Viewbox>
                                                                </Grid>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="UIElement.IsMouseOver" Value="True">
                                                                        <Setter TargetName="ExpandPath" Property="Shape.Stroke" Value="{DynamicResource MahApps.Brushes.Highlight}" />
                                                                        <!--<Setter TargetName="ExpandPath" Property="Shape.Fill" Value="#00FFFFFF" />-->
                                                                    </Trigger>
                                                                    <Trigger Property="ToggleButton.IsChecked" Value="True">
                                                                        <Setter TargetName="ExpandPath" Property="UIElement.RenderTransform">
                                                                            <Setter.Value>
                                                                                <RotateTransform Angle="90" CenterX="12" CenterY="12" />
                                                                            </Setter.Value>
                                                                        </Setter>
                                                                        <!--<Setter TargetName="ExpandPath" Property="Shape.Fill" Value="#FF595959" />-->
                                                                        <!--<Setter TargetName="ExpandPath" Property="Shape.Stroke" Value="#FF262626" />-->
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </ToggleButton.Style>
                                        </ToggleButton>
                                        <Border x:Name="Bd"
                                            HorizontalAlignment="Stretch"
                                            BorderThickness="{TemplateBinding Border.BorderThickness}"
                                            BorderBrush="{TemplateBinding Border.BorderBrush}"
                                            Padding="{TemplateBinding Control.Padding}"
                                            Background="{TemplateBinding Panel.Background}"
                                            SnapsToDevicePixels="True"
                                            Grid.Column="1">
                                            <ContentPresenter x:Name="PART_Header"
                                            Content="{TemplateBinding HeaderedContentControl.Header}"
                                            ContentTemplate="{TemplateBinding HeaderedContentControl.HeaderTemplate}"
                                            ContentStringFormat="{TemplateBinding HeaderedItemsControl.HeaderStringFormat}"
                                            ContentTemplateSelector="{TemplateBinding HeaderedItemsControl.HeaderTemplateSelector}"
                                            ContentSource="Header"
                                            HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}"
                                            SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" />
                                        </Border>
                                        <ItemsPresenter x:Name="ItemsHost" Grid.Column="1" Grid.Row="1" />
                                    </Grid>
                                </StackPanel>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="TreeViewItem.IsExpanded" Value="False">
                                        <Setter TargetName="ItemsHost" Property="UIElement.Visibility" Value="Collapsed" />
                                    </Trigger>
                                    <Trigger Property="ItemsControl.HasItems" Value="False">
                                        <Setter TargetName="Expander" Property="UIElement.Visibility" Value="Hidden" />
                                    </Trigger>
                                    <Trigger Property="TreeViewItem.IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Panel.Background" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" />
                                        <Setter Property="TextElement.Foreground"  Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}" />
                                    </Trigger>
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="TreeViewItem.IsSelected" Value="True" />
                                            <Condition Property="Selector.IsSelectionActive" Value="False" />
                                        </MultiTrigger.Conditions>
                                        <Setter TargetName="Bd"  Property="Panel.Background"  Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
                                        <Setter Property="TextElement.Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
                                    </MultiTrigger>
                                    <Trigger Property="UIElement.IsEnabled" Value="False">
                                        <Setter Property="TextElement.Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TreeView.ItemContainerStyle>
        </TreeView>

    </Grid>
</userControl:NotifiableUserControl>