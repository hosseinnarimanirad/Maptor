<UserControl x:Class="IRI.Maptor.Jab.Controls.View.MapLegendItemWithOptionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="root"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:model="clr-namespace:IRI.Maptor.Jab.Common.Models.Legend;assembly=IRI.Maptor.Jab.Common"
             xmlns:my="clr-namespace:IRI.Maptor.Jab.Controls.Assets.Converters"
             xmlns:mahApp="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro" 
             FontFamily="{StaticResource iranSans}"  >

    <Grid IsEnabled="{Binding IsInScaleRange}" Margin="0" >
        <Grid.Resources>
            <Style  TargetType="{x:Type Button}" x:Key="circleButton">
                <Setter Property="Margin" Value="4"/>
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Accent3}"/>
                <Setter Property="Width" Value="30"/>
                <Setter Property="Height" Value="30"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Grid>
                                <Ellipse Margin="1" Fill="{TemplateBinding Property=Background}"/>
                                <ContentPresenter/>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Accent}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <DataTemplate DataType="{x:Type model:LegendToggleCommand}">
                <ToggleButton VerticalAlignment="Center" Margin="2" Cursor="Hand" BorderThickness="0" Focusable="False" ToolTip="{Binding ToolTip}"
                              IsChecked="{Binding IsSelected, Mode=TwoWay}" IsEnabled="{Binding IsEnabled}"  
                              Visibility="{Binding IsCommandVisible, Converter={StaticResource boolToVisibilityConverter}}">
                    <ToggleButton.Style>
                        <Style TargetType="ToggleButton" BasedOn="{StaticResource {x:Type ToggleButton}}">
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Accent3}"/>
                            <Setter Property="Width" Value="30"/>
                            <Setter Property="Height" Value="30"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                                        <Grid>
                                            <Ellipse Margin="1" Fill="{TemplateBinding Property=Background}"/>
                                            <ContentPresenter/>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Accent}" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Accent}"/>
                                </Trigger>

                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="LightGray"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ToggleButton.Style>

                    <Path Stretch="Uniform" Fill="{Binding Foreground,RelativeSource={RelativeSource AncestorType=ToggleButton, Mode=FindAncestor}}" Width="16" Height="16" FlowDirection="LeftToRight">
                        <Path.Style>
                            <Style TargetType="{x:Type Path}" >
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=IsChecked, RelativeSource={RelativeSource AncestorType=ToggleButton, Mode=FindAncestor}}" Value="True">
                                        <Setter Property="Data" Value="{Binding PathMarkup}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Path=IsChecked, RelativeSource={RelativeSource AncestorType=ToggleButton, Mode=FindAncestor}}" Value="False">
                                        <Setter Property="Data" Value="{Binding NotSelectedPathMarkup}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Path.Style>
                    </Path>
                </ToggleButton>
            </DataTemplate>
            <DataTemplate DataType="{x:Type model:LegendCommand}">
                <Button Style="{StaticResource circleButton}" VerticalAlignment="Center" Margin="1" Cursor="Hand" Command="{Binding Command}" IsEnabled="{Binding IsEnabled}" 
                        BorderThickness="0" Focusable="False"  ToolTip="{Binding ToolTip}"
                        Visibility="{Binding IsCommandVisible, Converter={StaticResource boolToVisibilityConverter}}">
                    <Path Data="{Binding PathMarkup}" Stretch="Uniform" Fill="White" Width="16" Height="16" FlowDirection="LeftToRight"/>
                </Button>
            </DataTemplate>
            <my:ILayerToLegendConverter x:Key="iLayerToLegendConverter"/>
        </Grid.Resources>
        <!--<Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>-->
        <TextBlock MinWidth="30" VerticalAlignment="Center" Margin="4"  
                   Text="{Binding ElementName=root, Path=Title}" 
                   FontSize="{Binding ElementName=root, Path=TitleFontSize}"
                   Visibility="{Binding IsGroupLayer, Converter={StaticResource boolToVisibilityConverter}, ConverterParameter=True}"/>
        <StackPanel HorizontalAlignment="Stretch" Margin="0" Visibility="{Binding IsGroupLayer, Converter={StaticResource boolToVisibilityConverter}, ConverterParameter=False}">
            <StackPanel Orientation="Horizontal" Margin="0">
                <CheckBox Margin="2,0,0,0" IsChecked="{Binding Visibility, Mode=TwoWay, Converter={StaticResource visibilityToBoolConverter}}" Visibility="Visible"
                          VerticalAlignment="Center"  />
                <ToggleButton IsChecked="{Binding Visibility, Mode=TwoWay, Converter={StaticResource visibilityToBoolConverter}}" ClickMode="Press" Name="Expander"
                                Margin="2,0,0,0" Padding="0" Cursor="Hand" Visibility="Collapsed">
                    <ToggleButton.Style>
                        <Style TargetType="ToggleButton">
                            <Setter Property="UIElement.Focusable" Value="false" />
                            <Setter Property="FrameworkElement.Width" Value="24" />
                            <Setter Property="FrameworkElement.Height" Value="24" />
                            <Setter Property="Control.Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <iconPacks:PackIconMaterial VerticalAlignment="Center">
                                            <iconPacks:PackIconMaterial.Style>
                                                <Style TargetType="iconPacks:PackIconMaterial">
                                                    <Setter Property="Kind" Value="Eye" />
                                                    <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Accent}"/>
                                                    <Setter Property="Background" Value="Transparent"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ElementName=Expander, Path=IsChecked}" Value="True">
                                                            <Setter Property="Kind" Value="EyeOff" />
                                                        </DataTrigger>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Highlight}"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </iconPacks:PackIconMaterial.Style>
                                        </iconPacks:PackIconMaterial>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ToggleButton.Style>
                </ToggleButton>

                <Border Margin="0,0,2,0" Width="18" Height="16" BorderThickness="0" Cursor="Hand" CornerRadius="2"
                        Visibility="{Binding IsSymbolizable, Converter={StaticResource boolToVisibilityConverter}}">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="BorderBrush" Value="LightGray"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="BorderBrush" Value="DarkGray"/>
                                    <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Accent3}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Border.InputBindings>
                        <MouseBinding MouseAction="LeftClick" Command="{Binding ChangeSymbologyCommand}" CommandParameter="{Binding}"/>
                    </Border.InputBindings>
                    <!--<Grid>-->
                    <Path Margin="1" Data="{Binding Type, Converter={StaticResource iLayerToLegendConverter}}" x:Name="OriginalPath"
                            Fill="{Binding ElementName=root, Path=Symbology.Fill}" Stroke="{Binding ElementName=root, Path=Symbology.Stroke}"
                            StrokeThickness="{Binding ElementName=root, Path=Symbology.StrokeThickness}"
                            HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="{Binding Opacity}">
                        <!--<Path.RenderTransform>
                                <TranslateTransform X="0"/>
                            </Path.RenderTransform>-->
                    </Path>

                    <!--<iconPacks:PackIconMaterial x:Name="HoverIcon"
                                  Kind="Palette"
                                  Foreground="{DynamicResource MahApps.Brushes.Accent}"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Opacity="0">
                            -->
                    <!--<iconPacks:PackIconMaterial.RenderTransform>
                                <TranslateTransform X="-10"/>
                            </iconPacks:PackIconMaterial.RenderTransform>-->
                    <!--
                        </iconPacks:PackIconMaterial>-->
                    <!--</Grid>-->
                    <!--<Border.Triggers>-->
                    <!-- On MouseOver, fade out Path and fade in Icon -->
                    <!--EventTrigger RoutedEvent="Mouse.MouseEnter">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetName="OriginalPath"
                                     Storyboard.TargetProperty="Opacity"
                                     To="0" Duration="0:0:0.2"/>
                                    <DoubleAnimation Storyboard.TargetName="HoverIcon"
                                     Storyboard.TargetProperty="Opacity"
                                     To="1" Duration="0:0:0.2"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>


                        -->
                    <!-- On MouseLeave, reverse -->
                    <!--
                        <EventTrigger RoutedEvent="Mouse.MouseLeave">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetName="OriginalPath"
                                     Storyboard.TargetProperty="Opacity"
                                     To="1" Duration="0:0:0.2"/>
                                    <DoubleAnimation Storyboard.TargetName="HoverIcon"
                                     Storyboard.TargetProperty="Opacity"
                                     To="0" Duration="0:0:0.2"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>-->


                    <!--<EventTrigger RoutedEvent="Mouse.MouseEnter">
                            <BeginStoryboard>
                                <Storyboard>
                                    -->
                    <!-- Path fades out and moves slightly right -->
                    <!--
                                    <DoubleAnimation Storyboard.TargetName="OriginalPath"
                                     Storyboard.TargetProperty="Opacity"
                                     To="0" Duration="0:0:0.25"/>
                                    <DoubleAnimation Storyboard.TargetName="OriginalPath"
                                     Storyboard.TargetProperty="RenderTransform.(TranslateTransform.X)"
                                     To="20" Duration="0:0:0.25"/>

                                    -->
                    <!-- Icon fades in and slides in from left -->
                    <!--
                                    <DoubleAnimation Storyboard.TargetName="HoverIcon"
                                     Storyboard.TargetProperty="Opacity"
                                     To="1" Duration="0:0:0.25"/>
                                    <DoubleAnimation Storyboard.TargetName="HoverIcon"
                                     Storyboard.TargetProperty="RenderTransform.(TranslateTransform.X)"
                                     To="0" Duration="0:0:0.25"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>-->

                    <!-- On MouseLeave: reverse animation -->
                    <!--<EventTrigger RoutedEvent="Mouse.MouseLeave">
                            <BeginStoryboard>
                                <Storyboard>
                                    -->
                    <!-- Path fades back in and resets position -->
                    <!--
                                    <DoubleAnimation Storyboard.TargetName="OriginalPath"
                                     Storyboard.TargetProperty="Opacity"
                                     To="1" Duration="0:0:0.25"/>
                                    <DoubleAnimation Storyboard.TargetName="OriginalPath"
                                     Storyboard.TargetProperty="RenderTransform.(TranslateTransform.X)"
                                     To="0" Duration="0:0:0.25"/>

                                    -->
                    <!-- Icon fades out and slides back left -->
                    <!--
                                    <DoubleAnimation Storyboard.TargetName="HoverIcon"
                                     Storyboard.TargetProperty="Opacity"
                                     To="0" Duration="0:0:0.25"/>
                                    <DoubleAnimation Storyboard.TargetName="HoverIcon"
                                     Storyboard.TargetProperty="RenderTransform.(TranslateTransform.X)"
                                     To="-20" Duration="0:0:0.25"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>-->
                    <!--</Border.Triggers>-->
                </Border>
                <Slider x:Name="opacitySlider" VerticalAlignment="Center" Width="50" Minimum="0" Maximum="1" SmallChange=".1" LargeChange=".2"                         
                            TickFrequency=".2" Value="{Binding Opacity, Mode=TwoWay}"/>
                <TextBlock MinWidth="30" VerticalAlignment="Center" Margin="2" 
                            Text="{Binding ElementName=root, Path=Title}" 
                            FontSize="{Binding ElementName=root, Path=TitleFontSize}"
                            Visibility="{Binding ElementName=root, Path=IsEditable, Converter={StaticResource boolToVisibilityConverter}, ConverterParameter=False}"/>
                <TextBlock VerticalAlignment="Center"
                            Text="{Binding NumberOfSelectedFeatures, Converter={StaticResource latinDigitsToFarsiDigitsConverter}, StringFormat={}({0})}" 
                            FontSize="{Binding ElementName=root, Path=TitleFontSize}"
                            Visibility="{Binding NumberOfSelectedFeatures, Converter={StaticResource intGreaterThanIntParameterToVisibilityConverter}, ConverterParameter=0}"/>
                <TextBox BorderThickness="1" Padding="0" VerticalAlignment="Center" MinWidth="70"
                            Text="{Binding ElementName=root, Path=Title, Mode=TwoWay}" 
                            FontSize="{Binding ElementName=root, Path=TitleFontSize}"
                            Visibility="{Binding ElementName=root, Path=IsEditable, Converter={StaticResource boolToVisibilityConverter}}"
                            mahApp:TextBoxHelper.Watermark="عنوان" Margin="1" mahApp:TextBoxHelper.SelectAllOnFocus="True">
                    <TextBox.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="4"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Margin" Value="1"/>
                            <Setter Property="Padding" Value="0"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
                
                <ToggleButton ClickMode="Release"  Name="palette" Margin="0" Padding="0" Cursor="Hand" Background="Transparent" VerticalAlignment="Center"
                              Width="16" Height="16" Visibility="{Binding HasMultiSymbolizers, Converter={StaticResource boolToVisibilityConverter}}">
                    <ToggleButton.Style>
                        <Style TargetType="ToggleButton">
                            <Setter Property="UIElement.Focusable" Value="false" />
                            <Setter Property="FrameworkElement.Width" Value="24" />
                            <Setter Property="FrameworkElement.Height" Value="24" />
                            <Setter Property="Control.Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <iconPacks:PackIconMaterial VerticalAlignment="Center" Margin="0">
                                            <iconPacks:PackIconMaterial.Style>
                                                <Style TargetType="iconPacks:PackIconMaterial">
                                                    <Setter Property="Kind" Value="PaletteOutline" />
                                                    <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Accent}"/>
                                                    <Setter Property="Background" Value="Transparent"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ElementName=palette, Path=IsChecked}" Value="True">
                                                            <Setter Property="Kind" Value="Palette" />
                                                        </DataTrigger>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Highlight}"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </iconPacks:PackIconMaterial.Style>
                                        </iconPacks:PackIconMaterial>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ToggleButton.Style>
                </ToggleButton>

              
            </StackPanel>
            <ItemsControl Visibility="{Binding IsChecked, ElementName=root, Converter={StaticResource boolToVisibilityConverter}}" BorderBrush="Black" BorderThickness="0,0,0,0"
                            Margin="6,0,0,0" ItemsSource="{Binding ElementName=root, Path=Commands}" >
                <!--<ItemsControl.Resources>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="Transparent">
                                        <ContentPresenter/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="BorderThickness" Value="0"/> 
                    </Style>
                </ItemsControl.Resources>-->

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </StackPanel>

    </Grid>
</UserControl>
