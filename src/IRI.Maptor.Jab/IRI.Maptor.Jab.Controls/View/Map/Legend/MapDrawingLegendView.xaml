<userControl:NotifiableUserControl x:Class="IRI.Maptor.Jab.Controls.View.MapDrawingLegendView"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:userControl="clr-namespace:IRI.Maptor.Jab.Common;assembly=IRI.Maptor.Jab.Common" 
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                xmlns:localization="clr-namespace:IRI.Maptor.Jab.Common.Localization;assembly=IRI.Maptor.Jab.Common"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
                mc:Ignorable="d"  x:Name="root"
                xmlns:maptor="clr-namespace:IRI.Maptor.Jab.Controls.View" >
    <Grid>
        <Grid.Resources>
            <ObjectDataProvider x:Key="Localization" ObjectInstance="{x:Static localization:LocalizationManager.Instance}"/>
            <Style  TargetType="{x:Type Button}" x:Key="menuCircleButton">
                <Setter Property="Margin" Value="1"/>
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Accent}"/>
                <Setter Property="Width" Value="32"/>
                <Setter Property="Height" Value="32"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Focusable" Value="False"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Grid>
                                <Ellipse Margin="1" Fill="{TemplateBinding Property=Background}"/>
                                <ContentPresenter/>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Highlight}"/>
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Border.NonActive}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style TargetType="{x:Type iconPacks:PackIconModern}">
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Width" Value="16"/>
            </Style>
            <Style TargetType="{x:Type iconPacks:PackIconMaterial}">
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Width" Value="16"/>
            </Style>
            <Style TargetType="ToggleButton">
                <Setter Property="BorderThickness" Value="4,0,0,0"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalAlignment}" VerticalAlignment="{TemplateBinding VerticalAlignment}"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsChecked" Value="True">

                        <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.Highlight}"/>
                        <Setter Property="Cursor" Value="Arrow"/>
                    </Trigger>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Accent4}" />
                        <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.Highlight}"/>
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Foreground" Value="LightGray"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style TargetType="Expander" x:Key="toggleHorizontalExpander">
                <Setter Property="ExpandDirection" Value="Right"/>
                <Setter Property="Margin" Value="8,0,0,0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Expander">
                            <Border x:Name="expanderBorder"
                                    BorderBrush="{DynamicResource MahApps.Brushes.Highlight}"
                                    BorderThickness="1" Background="{DynamicResource MahApps.Brushes.Accent2}"
                                    Margin="1" Padding="0" CornerRadius="18">
                                <StackPanel Orientation="Horizontal">
                                    <ToggleButton x:Name="HeaderSite"
                                      Margin="1"                                      
                                      Width="32" Height="32"
                                      IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                        <ToggleButton.Style >
                                            <Style TargetType="ToggleButton" BasedOn="{StaticResource MahApps.Styles.ToggleButton.Circle}">
                                                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Accent3}"/>
                                                <Setter Property="BorderThickness" Value="0"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Highlight}"/>
                                                        <Setter Property="Opacity" Value="1"/>
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Highlight}"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ToggleButton.Style>
                                        <ContentPresenter ContentSource="Header"/>
                                    </ToggleButton>
                                    <iconPacks:PackIconMaterial Kind="ChevronDoubleLeft" Width="8" Height="8" HorizontalAlignment="Center" Margin="4,0,0,0"
                                        Visibility="{Binding IsExpanded, RelativeSource={RelativeSource AncestorType=Expander}, Converter={StaticResource boolToVisibilityConverter}}"/>
                                    <iconPacks:PackIconMaterial Kind="ChevronDoubleRight" Width="8" Height="8" HorizontalAlignment="Center" Margin="0,0,8,0"
                                        Visibility="{Binding IsExpanded, RelativeSource={RelativeSource AncestorType=Expander}, Converter={StaticResource boolToVisibilityConverter}, ConverterParameter=False}"/>

                                    <ContentPresenter x:Name="ExpandSite"
                                              ContentSource="Content"
                                              Margin="5,0,0,0"
                                              RenderTransformOrigin="0,0.5"
                                              Visibility="Collapsed">
                                    </ContentPresenter>

                                </StackPanel>
                            </Border>

                            <ControlTemplate.Triggers>
                                <Trigger Property="IsExpanded" Value="True">
                                    <Setter TargetName="ExpandSite" Property="Visibility" Value="Visible"/>
                                    <Setter TargetName="HeaderSite" Property="Background" Value="{DynamicResource MahApps.Brushes.Highlight }"/>
                                </Trigger>

                                <Trigger Property="IsExpanded" Value="False">
                                    <Setter TargetName="ExpandSite" Property="Visibility" Value="Collapsed"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style TargetType="Path" x:Key="buttonPath">
                <Setter Property="Stretch" Value="Uniform"/>
                <Setter Property="Fill" Value="White"/>
                <Setter Property="Width" Value="18"/>
                <Setter Property="Height" Value="18"/>
                <Setter Property="FlowDirection" Value="LeftToRight"/>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ScrollViewer HorizontalScrollBarVisibility="Auto"
                        VerticalScrollBarVisibility="Auto"
                        Visibility="{Binding ElementName=root, Path=ShowTools, Converter={StaticResource boolToVisibilityConverter}}">
            <DockPanel HorizontalAlignment="Center">
                <Button Style="{StaticResource menuCircleButton}" Command="{Binding MoveDrawingItemDownCommand}"  
                        ToolTip="{Binding [cmd_drawingLegend_moveDown], Source={StaticResource Localization}}" 
                        IsEnabled="{Binding CanMoveDrawingItemDown}">
                    <iconPacks:PackIconModern Kind="LayerDown" />
                </Button>
                <Button Style="{StaticResource menuCircleButton}" Command="{Binding MoveDrawingItemUpCommand}"  
                        ToolTip="{Binding [cmd_drawingLegend_moveUp], Source={StaticResource Localization}}" 
                        IsEnabled="{Binding CanMoveDrawingItemUp}">
                    <iconPacks:PackIconModern Kind="LayerUp" />
                </Button>

                <Expander Style="{StaticResource toggleHorizontalExpander}" 
                            IsExpanded="{Binding ElementName=root, Path=SelectedExpanderIndex, Converter={StaticResource indexToBoolConverter}, ConverterParameter=1}">
                    <Expander.Header>
                        <iconPacks:PackIconModern Kind="Add"/>
                    </Expander.Header>
                    <StackPanel Orientation="Horizontal">
                        <Button Style="{StaticResource menuCircleButton}" Command="{Binding AddGeoJsonToDrawingItemsCommand}"  
                                ToolTip="{Binding [cmd_drawingLegend_addGeoJson], Source={StaticResource Localization}}">
                            <Path Style="{StaticResource buttonPath}" Data="{StaticResource json}"/>
                        </Button>
                        <Button Style="{StaticResource menuCircleButton}" Command="{Binding AddLongLatTxtToDrawingItemsCommand}"  
                                ToolTip="{Binding [cmd_drawingLegend_addLatLongTxt], Source={StaticResource Localization}}">
                            <Path Style="{StaticResource buttonPath}" Data="{StaticResource txt}"/>
                        </Button>
                        <Button Style="{StaticResource menuCircleButton}" Command="{Binding AddShapefileToDrawingItemsCommand}"  
                                ToolTip="{Binding [cmd_drawingLegend_addShapefile], Source={StaticResource Localization}}">
                            <Path Style="{StaticResource buttonPath}" Data="{StaticResource shapefile}"/>
                        </Button>
                    </StackPanel>
                </Expander>

                <Expander Style="{StaticResource toggleHorizontalExpander}"  
                            IsExpanded="{Binding ElementName=root, Path=SelectedExpanderIndex, Converter={StaticResource indexToBoolConverter}, ConverterParameter=2}">
                    <Expander.Header>
                        <iconPacks:PackIconModern Kind="VectorPenAdd"/>
                    </Expander.Header>
                    <StackPanel Orientation="Horizontal">
                        <Button Style="{StaticResource menuCircleButton}" Command="{Binding DrawPointCommand}" 
                                ToolTip="{Binding [cmd_general_drawPoint], Source={StaticResource Localization}}"  >
                            <iconPacks:Material Kind="VectorPointPlus"/>
                        </Button>
                        <Button Style="{StaticResource menuCircleButton}" Command="{Binding DrawPolylineCommand}" 
                                ToolTip="{Binding [cmd_general_drawPolyline], Source={StaticResource Localization}}">
                            <iconPacks:Material Kind="VectorPolylinePlus"/>
                        </Button>
                        <Button Style="{StaticResource menuCircleButton}" Command="{Binding DrawPolygonCommand}" 
                                ToolTip="{Binding [cmd_general_drawPolygon], Source={StaticResource Localization}}">
                            <iconPacks:Material Kind="VectorSquarePlus"/>
                        </Button>
                        <Button Style="{StaticResource menuCircleButton}" Command="{Binding AddTextToMapCommand}" 
                                ToolTip="{Binding [cmd_general_addTextToMap], Source={StaticResource Localization}}">
                            <iconPacks:Material Kind="FormatAnnotationPlus"/>
                        </Button>
                    </StackPanel>
                </Expander>


            </DockPanel>
        </ScrollViewer>
        <Line Grid.Row="1" X1="0" X2="100" StrokeThickness="2" Stroke="{DynamicResource MahApps.Brushes.Highlight}" 
              Visibility="{Binding ElementName=root, Path=ShowTools, Converter={StaticResource boolToVisibilityConverter}}"
              Opacity="1" Margin="0,4"
              Stretch="Fill"/>
        <ListBox Grid.Row="2" ItemsSource="{Binding DrawingItems}" SelectionMode="Single" SelectedItem="{Binding SelectedDrawingItem, Mode=TwoWay}">
            <ListBox.ItemContainerStyle>
                <!-- Style to get rid of the selection visual -->
                <Style TargetType="{x:Type ListBoxItem}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="IsSelected" Value="{Binding IsSelectedInToc, Mode=TwoWay}" />
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <RadioButton Style="{StaticResource {x:Type ToggleButton}}" GroupName="{Binding ElementName=root, Path=GroupName}" DataContext="{Binding }"
                                 IsChecked="{Binding IsSelectedInToc, Mode=TwoWay}">
                        <maptor:MapLegendItemWithOptionsView Title="{Binding LayerName, Mode=TwoWay}" Symbology="{Binding DefaultSymbology}"  
                                                                     IsEditable="True" Commands="{Binding Commands}" 
                                                                     IsChecked="{Binding IsSelectedInToc}"
                                                                     TitleFontSize="{Binding ElementName=root, Path=TitleFontSize}"/>
                    </RadioButton>

                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</userControl:NotifiableUserControl>
