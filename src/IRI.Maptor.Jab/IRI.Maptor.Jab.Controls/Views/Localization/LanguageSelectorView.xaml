<UserControl x:Class="IRI.Maptor.Jab.Controls.Views.Localization.LanguageSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:localization="clr-namespace:IRI.Maptor.Jab.Common.Localization;assembly=IRI.Maptor.Jab.Common"
             FlowDirection="{Binding CurrentFlowDirection, Source={StaticResource Localization}}">
    <UserControl.Resources>
        <ObjectDataProvider x:Key="Localization" ObjectInstance="{x:Static localization:LocalizationManager.Instance}"/>
    </UserControl.Resources>
    <Border Background="White" Padding="8"  
            Effect="{DynamicResource MahApps.Effects.DropShadow}">
        <!--BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
            BorderThickness="1"
            CornerRadius="4"-->
        <StackPanel>
            <TextBlock Text="{Binding [languageSelector_title], Source={StaticResource Localization}}" FontWeight="SemiBold" Margin="0,0,0,8"
                       FontSize="14"/>
            <ListBox ItemsSource="{Binding AvailableLanguages}"
                     SelectedItem="{Binding SelectedLanguage, Mode=TwoWay}"
                     BorderThickness="0" Background="Transparent"
                     Padding="0">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="3"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="8,6"/>
                        <Setter Property="Margin" Value="0,2"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="IsEnabled" Value="{Binding IsEnabled}"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Accent3}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Accent2}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type localization:LanguageItem}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Image Source="{Binding FlagUri}" Stretch="Uniform" Width="24" Height="18"
                                   VerticalAlignment="Center" Margin="4,0,4,0"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding EnglishName}" FontWeight="SemiBold" FontSize="12" FlowDirection="LeftToRight"/>
                                <TextBlock Text="{Binding NativeName}" FontSize="11" Foreground="Gray"
                                           FlowDirection="{Binding TextFlowDirection}"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Border>
</UserControl>
