<UserControl x:Class="IRI.Maptor.Jab.Controls.Symbology.Sld.SimpleFilterEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mahApp="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:vm="clr-namespace:IRI.Maptor.Jab.Wpf.ViewModels.Symbology"
             mc:Ignorable="d"
             d:DesignHeight="240" d:DesignWidth="400">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <CheckBox Grid.Row="0" Margin="4"
                  Content="{Binding [sldEditor_filter_enableFilter], Source={StaticResource Localization}}"
                  IsChecked="{Binding HasFilter, Mode=TwoWay}"/>

        <GroupBox Grid.Row="1" Header="{Binding [sldEditor_filter_filterSettings], Source={StaticResource Localization}}" Margin="4"
                  IsEnabled="{Binding HasFilter}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding [sldEditor_filter_property], Source={StaticResource Localization}}" VerticalAlignment="Center" Margin="4"/>
                <TextBox Grid.Row="0" Grid.Column="1" Margin="4"
                         Text="{Binding FilterPropertyName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding [sldEditor_filter_operator], Source={StaticResource Localization}}" VerticalAlignment="Center" Margin="4"/>
                <ComboBox Grid.Row="1" Grid.Column="1" Margin="4" SelectedItem="{Binding FilterOperator, Mode=TwoWay}">
                    <ComboBox.ItemsSource>
                        <x:Array Type="{x:Type vm:FilterComparisonOperator}">
                            <vm:FilterComparisonOperator>Equal</vm:FilterComparisonOperator>
                            <vm:FilterComparisonOperator>NotEqual</vm:FilterComparisonOperator>
                            <vm:FilterComparisonOperator>LessThan</vm:FilterComparisonOperator>
                            <vm:FilterComparisonOperator>GreaterThan</vm:FilterComparisonOperator>
                            <vm:FilterComparisonOperator>LessThanOrEqual</vm:FilterComparisonOperator>
                            <vm:FilterComparisonOperator>GreaterThanOrEqual</vm:FilterComparisonOperator>
                        </x:Array>
                    </ComboBox.ItemsSource>
                </ComboBox>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="{Binding [sldEditor_filter_value], Source={StaticResource Localization}}" VerticalAlignment="Center" Margin="4"/>
                <TextBox Grid.Row="2" Grid.Column="1" Margin="4"
                         Text="{Binding FilterValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="4,8,4,2"
                           FontStyle="Italic" Foreground="Gray"
                           TextWrapping="Wrap"
                           Text="{Binding FilterDescription}"/>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
