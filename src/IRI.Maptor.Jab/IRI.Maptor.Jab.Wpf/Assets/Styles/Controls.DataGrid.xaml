<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/IRI.Maptor.Jab.Wpf;component/Assets/Styles/Common.Metrics.xaml"/>
        <ResourceDictionary Source="/IRI.Maptor.Jab.Wpf;component/Assets/Styles/Controls.TextBlock.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!--The shared read-list grid. WPF's stock DataGrid chrome (blue selection, full gridlines,
        square headers) does not belong to this design system, so every list view was rendering
        as raw WPF. Use IRI.Maptor.Styles.DataGrid and the row/cell/header styles come with it.

        The cell carries its own template on purpose: MahApps paints selection from inside the
        cell template, and template triggers outrank style triggers, so a themed selection colour
        is only reachable by owning the template.-->

    <Style x:Key="IRI.Maptor.Styles.DataGrid.ColumnHeader"
           TargetType="DataGridColumnHeader"
           BasedOn="{StaticResource MahApps.Styles.DataGridColumnHeader}">
        <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Gray9}"/>
        <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Text}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.Gray8}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="8,7"/>
        <Setter Property="MinHeight" Value="32"/>
        <Setter Property="SeparatorBrush" Value="{DynamicResource MahApps.Brushes.Gray8}"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
    </Style>

    <Style x:Key="IRI.Maptor.Styles.DataGrid.Cell" TargetType="DataGridCell">
        <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Text}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Padding" Value="8,5"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border x:Name="cellRoot"
                            Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter VerticalAlignment="Center"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!--selection is painted per cell so it covers the whole row width-->
                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=DataGridRow}}" Value="True">
                            <Setter TargetName="cellRoot" Property="Background"
                                    Value="{DynamicResource MahApps.Brushes.Accent4}"/>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="IRI.Maptor.Styles.DataGrid.Row" TargetType="DataGridRow">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="MinHeight" Value="30"/>
        <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Text}"/>
        <Style.Triggers>
            <!--zebra striping: AlternationCount is set by IRI.Maptor.Styles.DataGrid-->
            <Trigger Property="AlternationIndex" Value="1">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Gray10}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Gray9}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="IRI.Maptor.Styles.DataGrid" TargetType="DataGrid">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="RowBackground" Value="Transparent"/>
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
        <Setter Property="CanUserResizeRows" Value="False"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource MahApps.Brushes.Gray8}"/>
        <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
        <Setter Property="AlternationCount" Value="2"/>
        <Setter Property="SelectionUnit" Value="FullRow"/>
        <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ColumnHeaderStyle" Value="{StaticResource IRI.Maptor.Styles.DataGrid.ColumnHeader}"/>
        <Setter Property="RowStyle" Value="{StaticResource IRI.Maptor.Styles.DataGrid.Row}"/>
        <Setter Property="CellStyle" Value="{StaticResource IRI.Maptor.Styles.DataGrid.Cell}"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Source={StaticResource Localization}, Path=IsPersian}" Value="True">
                <Setter Property="FontFamily" Value="{DynamicResource iranSans}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!--read-only variant: the common case for queue/history lists-->
    <Style x:Key="IRI.Maptor.Styles.DataGrid.ReadOnly"
           TargetType="DataGrid"
           BasedOn="{StaticResource IRI.Maptor.Styles.DataGrid}">
        <Setter Property="IsReadOnly" Value="True"/>
    </Style>

</ResourceDictionary>
