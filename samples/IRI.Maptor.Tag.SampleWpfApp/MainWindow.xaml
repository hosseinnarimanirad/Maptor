<Window x:Class="IRI.Maptor.Tag.SampleWpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mahApp="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"  
        xmlns:maptor="clr-namespace:IRI.Maptor.Jab.Controls.View;assembly=IRI.Maptor.Jab.Controls" 
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:localization="clr-namespace:IRI.Maptor.Jab.Common.Localization;assembly=IRI.Maptor.Jab.Common"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" Loaded="Window_Loaded" 
        Title="MainWindow" Height="600" Width="1100" x:Name="root" >
    <Window.Resources>
        <ObjectDataProvider x:Key="Localization" ObjectInstance="{x:Static localization:LocalizationManager.Instance}"/>
        <Style TargetType="{x:Type TabItem}">
            <Setter Property="mahApp:HeaderedControlHelper.HeaderFontSize" Value="15"></Setter>
        </Style>
    </Window.Resources>
    <Grid FlowDirection="{Binding CurrentFlowDirection, Source={StaticResource Localization}}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="200"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <maptor:LanguageCombo/>
        <!--<ComboBox ItemsSource="{Binding AvailableLanguages}"
              SelectedItem="{Binding SelectedLanguage}"
              HorizontalContentAlignment="Center" VerticalAlignment="Center">
            <ComboBox.DataContext>
                <localization:LanguageSelectorViewModel/>
            </ComboBox.DataContext>
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="3">
                        <Image Source="{Binding FlagUri}" Stretch="Uniform" Width="34" Height="24" Margin="0,0,5,0"/>
                        <TextBlock Text="{Binding NativeName}" VerticalAlignment="Center" FlowDirection="{Binding TextFlowDirection}"/>
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>-->

        <StackPanel Grid.Column="1">
            <StackPanel.Resources>
                <Style TargetType="Button" BasedOn="{StaticResource flatButtonPrimary}">
                    <Setter Property="Margin" Value="4"/>
                </Style>
            </StackPanel.Resources>
            <Border BorderBrush="Black" BorderThickness="1" Margin="2">
                <StackPanel Orientation="Horizontal" >
                    <Button Content="{Binding [cmd_general_clearAll], Source={StaticResource Localization}}" Command="{Binding ClearAllCommand}"/>
                    <Button Content="{Binding [cmd_general_fullExtent], Source={StaticResource Localization}}" Command="{Binding FullExtentCommand}"/>
                    <Button Content="{Binding [cmd_general_addShapefile], Source={StaticResource Localization}}" Command="{Binding AddShapefileCommand}"/>
                    <Button Content="{Binding [cmd_general_measureLength], Source={StaticResource Localization}}" Command="{Binding MeasureLengthCommand}"/>
                    <Button Content="{Binding [cmd_general_measureArea], Source={StaticResource Localization}}" Command="{Binding MeasureAreaCommand}"/>
                    <Button Content="{Binding [cmd_general_drawPoint], Source={StaticResource Localization}}" Command="{Binding DrawPointCommand}"/>
                    <Button Content="{Binding [cmd_general_drawPolyline], Source={StaticResource Localization}}" Command="{Binding DrawPolylineCommand}"/>
                    <Button Content="{Binding [cmd_general_drawPolygon], Source={StaticResource Localization}}" Command="{Binding DrawPolygonCommand}"/>
                    <Button Content="{Binding [cmd_general_addTextToMap], Source={StaticResource Localization}}" Command="{Binding AddTextToMapCommand}"/>
                    <Button Content="{Binding [cmd_general_goTo], Source={StaticResource Localization}}" Command="{Binding GoToCommand}"/>
                </StackPanel>
            </Border>
            <Border BorderBrush="Black" BorderThickness="1" Margin="2">
                <StackPanel Orientation="Horizontal">
                    <Button Content="Add Marker Sample" Command="{Binding ShowSpecialPointLayerSampleCommand}"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <maptor:MapViewer Grid.Row="1" Grid.Column="1" x:Name="map" BorderBrush="Black" BorderThickness="1"/>

        <maptor:MapInfoView Grid.Row="1" Grid.Column="1" DataContext="{Binding }"/>

        <maptor:Scalebar Grid.Row="1" Grid.Column="1" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="4"  
                         CurrentScale="{Binding CurrentPointInverseMapScale}" ShowScaleValue="True" />
        
        <maptor:CoordinatePanelView 
            Grid.Column="1" 
            Grid.Row="1" 
            Margin="10"  
            HorizontalAlignment="Right" 
            VerticalAlignment="Bottom"
            DataContext="{Binding CoordinatePanel}" 
            Position="{Binding CurrentPoint, ElementName=map}"
            ZoomLevel="{Binding ElementName=root, Path=DataContext.CurrentZoomLevel}"/>

        <StackPanel Grid.Row="1">
            <Expander Header="{Binding [ui_header_baseMaps], Source={StaticResource Localization}}" DockPanel.Dock="Top" IsExpanded="True"  Margin="0,1,0,0" Cursor="Hand">
                <ComboBox ItemsSource="{Binding MapProviders}" SelectedItem="{Binding SelectedMapProvider, Mode=TwoWay}" VerticalAlignment="Center" DisplayMemberPath="Title"/>
            </Expander>
            <Expander Header="{Binding [ui_header_drawingLegend], Source={StaticResource Localization}}" DockPanel.Dock="Top" IsExpanded="True"  Margin="0,1,0,0" Cursor="Hand">
                <maptor:MapDrawingLegendView MinHeight="90"/>
            </Expander>
            <Expander Header="{Binding [ui_header_layerLegend], Source={StaticResource Localization}}" IsExpanded="True" Margin="0,1,0,0" Cursor="Hand">
                <maptor:MapLegendView EnableFilterMode="True"  Layers="{Binding Layers}"  GroupName="B"/>
            </Expander>
        </StackPanel>

        <Expander Header="Attributes" Background="White" VerticalAlignment="Bottom" MaxHeight="350" Grid.Row="2" Grid.ColumnSpan="2" >
            <TabControl ItemsSource="{Binding SelectedLayers}" SelectedIndex="0" Margin="0" Padding="0" 
                        VerticalAlignment="Stretch" SelectedItem="{Binding CurrentLayer, Mode=TwoWay}">
                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0">
                            <TextBlock Margin="0" VerticalAlignment="Center">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0} ({1})">
                                        <Binding Path="LayerName"/>
                                        <Binding Path="Features.Count" Converter="{StaticResource localizedNumberConverter}"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                            <Button Command="{Binding RemoveCommand}" Style="{x:Null}" Cursor="Hand" Margin="8,2,2,2" 
                                    Height="15" Padding="2" VerticalAlignment="Center" BorderThickness="0">
                                <Viewbox>
                                    <iconPacks:PackIconModern Kind="Close" Foreground="{DynamicResource MahApps.Brushes.Accent}"/>
                                </Viewbox>
                            </Button>
                        </StackPanel>
                    </DataTemplate>
                </TabControl.ItemTemplate>
                <TabControl.ContentTemplate>
                    <DataTemplate>
                        <maptor:FeatureTable DataContext="{Binding }" CanUserEditGeometry="False" IsZoomToGeometryEnabled="True"/>
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl> 
        </Expander>
    </Grid>
</Window>
